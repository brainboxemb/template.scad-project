name: SCAD production

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: write
  packages: read

jobs:
  scad:
    uses: brainboxemb/tool.scad-project/.github/workflows/project-production.yml@78b5f4ec429381d7459e226cc04f354d5fe4394c
    with:
      aggregate_task: consumer:scad.ci
      cache_namespace: template-scad-production-m004-step3-v1
