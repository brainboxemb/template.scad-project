name: SCAD production

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: write
  packages: read

jobs:
  scad:
    uses: brainboxemb/tool.scad-project/.github/workflows/project-production.yml@fa3906a0589aecb8b957919c302bd81b1c674376
    with:
      aggregate_task: consumer:scad.ci
      cache_namespace: template-scad-production-m004-step3-v1
